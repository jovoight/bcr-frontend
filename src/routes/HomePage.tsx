import { Box, Typography } from '@mui/material';

export const HomePage = () => {
        return (
                <Box>
                        <Typography>Home Page</Typography>
                </Box>
        );
}
