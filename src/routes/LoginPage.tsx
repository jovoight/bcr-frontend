import { Box, Typography } from '@mui/material';

export const LoginPage = () => {
        return (
                <Box>
                        <Typography>Login Page</Typography>
                </Box>
        );
}
