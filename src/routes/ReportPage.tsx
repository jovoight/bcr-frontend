import { Box, Typography } from '@mui/material';

export const ReportPage = () => {
        return (
                <Box>
                        <Typography>Report Page</Typography>
                </Box>
        );
}
