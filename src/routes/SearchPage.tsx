import { Box, Typography } from '@mui/material';

export const SearchPage = () => {
        return (
                <Box>
                        <Typography>Search Page</Typography>
                </Box>
        );
}
