import { Box, Typography } from '@mui/material';

export const RentalPage = () => {
        return (
                <Box>
                        <Typography>Rental Page</Typography>
                </Box>
        );
}
